language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t scryptbrewer/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true scryptbrewer/react-test npm run test

after_success:
  - docker build -t scryptbrewer/multi-client ./client
  - docker build -t scryptbrewer/multi-nginx ./nginx
  - docker build -t scryptbrewer/multi-server ./server
  - docker build -t scryptbrewer/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push scryptbrewer/multi-client
  - docker push scryptbrewer/multi-nginx
  - docker push scryptbrewer/multi-server
  - docker push scryptbrewer/multi-worker


